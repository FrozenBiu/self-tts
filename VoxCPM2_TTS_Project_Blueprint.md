# Tài Liệu Đặc Tả Dự Án: Hệ Thống Text-to-Speech Cá Nhân (VoxCPM2)

## 1. Tổng Quan Dự Án (Project Overview)

Dự án xây dựng một ứng dụng web Text-to-Speech (TTS) cục bộ, phục vụ nhu cầu sử dụng cá nhân. Hệ thống sử dụng mô hình AI **VoxCPM2** nhằm giải quyết triệt để vấn đề đọc vấp, sai ngữ điệu khi văn bản có sự pha trộn giữa Tiếng Việt và Tiếng Anh (code-switching) - điểm yếu của các API TTS truyền thống. Hệ thống tận dụng sức mạnh xử lý của GPU NVIDIA (RTX 3060 12GB) để đạt tốc độ phản hồi tối ưu.

## 2. Kiến Trúc Hệ Thống (System Architecture)

Hệ thống được thiết kế theo mô hình **Client - Server** tách biệt hoàn toàn để đảm bảo hiệu năng, tránh nghẽn cổ chai bộ nhớ khi load mô hình AI, và dễ dàng mở rộng tính năng (như Voice Cloning) sau này.

- **AI Inference Engine (Backend):** Đảm nhiệm việc tải mô hình VoxCPM2 vào VRAM một lần duy nhất khi khởi động (Warm-up). Cung cấp RESTful API để nhận văn bản từ Client, xử lý qua GPU và trả về file âm thanh (hoặc stream âm thanh).
- **Web User Interface (Frontend):** Giao diện tương tác người dùng (SPA), nhận văn bản đầu vào, cấu hình thông số (tốc độ, ngữ điệu, voice design) và phát lại âm thanh kết quả.

## 3. Tech Stack (Công Nghệ Sử Dụng)

### 3.1. Backend (AI Core)

- **Ngôn ngữ & Framework:** Python 3.10+, **FastAPI**. (Lý do: Nhẹ, tốc độ cao, hỗ trợ xử lý bất đồng bộ native, tự động sinh tài liệu Swagger UI).
- **AI Model & ML Framework:**
  - **VoxCPM2** (HuggingFace/ModelScope).
  - **PyTorch** (Phiên bản hỗ trợ CUDA 12.4).
- **Server:** Uvicorn.
- **Database (Tùy chọn mở rộng):** MongoDB hoặc SQLite (Dùng để lưu lịch sử các đoạn hội thoại/văn bản đã chuyển đổi nếu cần).

### 3.2. Frontend (Web UI)

- **Core:** React (Khởi tạo qua Vite) + TypeScript.
- **Package Manager:** **pnpm** (Bắt buộc sử dụng cho mọi thao tác cài đặt package).
- **State Management:** **Zustand** (Quản lý state loading, dữ liệu form nhập liệu tinh gọn).
- **Styling & CSS Framework:** Tailwind CSS.
- **UI Components & Hiệu ứng:**
  - Ưu tiên sử dụng **ShadCn UI** và **HeroUI** cho các component cơ bản (Button, Input, Slider, Card, Form).
  - Tích hợp **MagicUI** và **Motion-Primitives** để tạo các animation mượt mà (ví dụ: hiệu ứng visualizer khi âm thanh đang phát, loading state).
  - Sử dụng **Pattern Craft** để trang trí background, tạo chiều sâu cho giao diện UI.
- **Thông báo (Toast/Alert):** Bắt buộc sử dụng **Sonner** để hiển thị trạng thái (Đang tạo âm thanh, Thành công, Lỗi...) thay thế hoàn toàn cho `alert()` mặc định của trình duyệt.

## 4. Cấu Trúc Thư Mục Dự Kiến (Directory Structure)

```text
TTS/
├── backend/                  # AI API Server (Python)
│   ├── main.py               # File khởi chạy FastAPI, load mô hình
│   ├── model_handler.py      # Module chứa logic gọi VoxCPM2 (generate)
│   ├── requirements.txt      # Chứa fastapi, uvicorn, torch, voxcpm...
│   └── outputs/              # Thư mục lưu tạm file .wav
└── frontend/                 # Web UI (React + pnpm)
    ├── package.json          # (Generated by pnpm create vite)
    ├── src/
    │   ├── components/       # Các component từ Shadcn UI, HeroUI, MagicUI
    │   ├── store/            # Chứa file store của Zustand
    │   ├── App.tsx           # Layout chính, form nhập liệu
    │   └── main.tsx          # Điểm neo ứng dụng, bọc Sonner Toaster
    ├── tailwind.config.js    # Cấu hình Tailwind, Pattern Craft, animation
    └── pnpm-lock.yaml
```

## 5. Lộ Trình Triển Khai (Implementation Phases)

### Giai đoạn 1: Khởi tạo Backend (AI Core)

1.  Setup môi trường ảo (virtualenv) cho Python.
2.  Cài đặt PyTorch (CUDA 12.4) và FastAPI.
3.  Tạo endpoint `POST /api/tts`. Khởi tạo instance VoxCPM2 lúc khởi động app (`@app.on_event("startup")`).
4.  Nhận payload (text, speed, style), gọi `model.generate()`, lưu ra `.wav` và trả về URL tĩnh (Static Files) hoặc trả trực tiếp dữ liệu dạng byte.

### Giai đoạn 2: Khởi tạo Frontend

1.  Chạy `pnpm create vite frontend --template react-ts`.
2.  Cài đặt Tailwind CSS.
3.  Setup các thư viện UI: `pnpm add zustand sonner`, cấu hình Shadcn UI/HeroUI.
4.  Tạo giao diện: Một khung nhập Text lớn, khu vực tùy chỉnh tham số (nếu có), và nút Submit. Bố trí component Sonner Toaster ở root.

### Giai đoạn 3: Tích hợp & Hoàn thiện UX

1.  Gọi API từ Frontend sang Backend (Sử dụng Fetch API hoặc Axios).
2.  Kích hoạt Sonner toast báo trạng thái khi đang fetch data.
3.  Sử dụng thẻ `<audio>` để phát trực tiếp file trả về. Áp dụng hiệu ứng từ Motion-Primitives để tạo phản hồi thị giác khi âm thanh phát.

## 6. Hướng Dẫn Cho AI Coding Assistant (Antigravity/Cursor)

- **Rule 1:** Tuân thủ tuyệt đối Tech Stack đã đề ra, không tự ý thay đổi thư viện UI hoặc package manager (luôn dùng pnpm).
- **Rule 2:** Khi đưa ra các script cài đặt, luôn cung cấp lệnh sử dụng `pnpm`.
- **Rule 3:** Khi xây dựng layout, hãy ưu tiên setup các component từ Shadcn UI, HeroUI, MagicUI, Motion-Primitives, và Pattern Craft theo sát yêu cầu. Không sử dụng hàm alert mặc định, luôn gọi Sonner để hiển thị thông báo.
- **Rule 4:** Đảm bảo xử lý CORS trên FastAPI để Frontend (chạy cổng Vite mặc định 5173) có thể giao tiếp mượt mà.
