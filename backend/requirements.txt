# ──────────────────────────────────────────────
# VoxCPM2 TTS Backend — Python Dependencies
# ──────────────────────────────────────────────

# API Framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# AI Model
voxcpm==2.0.3

# Audio I/O
soundfile==0.12.1

# Utilities
python-dotenv==1.0.1
pydantic==2.10.3

# NOTE: PyTorch (CUDA 12.4) phải cài riêng qua lệnh pip bên dưới
# Không đưa torch vào file này để tránh pip cài nhầm phiên bản CPU
